# torch / CUDA come from the base image -- do not install torch here.
# transformers >=4.56 is required: from_pretrained's dtype= argument is only
# the canonical name from that version on.
transformers>=4.56,<6
datasets>=3.0.0
accelerate>=1.0.0
deepspeed>=0.15.0
sentencepiece>=0.2.0
