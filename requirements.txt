# torch / CUDA come from the base image -- do not install torch here.
# The code targets the transformers v5 API: from_pretrained(dtype=...) and
# TrainingArguments(warmup_steps=<float ratio>), which replaced warmup_ratio.
transformers>=5.15,<6
datasets>=3.0.0
accelerate>=1.0.0
deepspeed>=0.15.0
sentencepiece>=0.2.0
# Only imported when REPORT_TO includes "wandb".
wandb>=0.18
